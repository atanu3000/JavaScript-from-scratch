// string indexing

let firstName = "Atanu Paul";

console.log(firstName[0]);  // first character of the string
console.log(firstName.length);  // length of the string
console.log(firstName[firstName.length - 1]); // last character of the string